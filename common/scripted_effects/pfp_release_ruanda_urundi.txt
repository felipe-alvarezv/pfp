#Scripted effect to release Ruanda-Urundi (Q66)
pfp_release_ruanda_urundi = {
	hidden_effect = {
		#If resistance mechanics are enabled, formable puppets will contest and control its corresponding states.
		if = {
			limit = { has_global_flag = pfp_management_resistance_enabled }

			#Add contestion to formable puppet
			768 = { add_contested_owner = Q66 } #Rwanda
			769 = { add_contested_owner = Q66 } #Burundi

			#Add claims to formable puppet
			768 = { add_claim_by = Q66 } #Rwanda
			769 = { add_claim_by = Q66 } #Burundi

			#Set formable puppet as controller of contested states
			every_state = {
				limit = { has_contested_owner = Q66 }

				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = {
					limit = { is_owned_by = ROOT }
					transfer_state_to = Q66
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					set_state_controller_to  = Q66
				}
			}

			#Set custom focus tree to combat resistance
			Q66 = { load_focus_tree = pfp_formable_puppet_focus }
		}
		#If resistance mechanics are disabled, formable puppets will control and core its corresponding states.
		else = {
			#Add cores to formable puppet
			768 = { add_core_of = Q66 } #Rwanda
			769 = { add_core_of = Q66 } #Burundi

			#Set formable puppet as owner and controller of core states
			every_state = {
				limit = { is_core_of = Q66 }

				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = {
					limit = { is_owned_by = ROOT }

					add_contested_owner = Q66
					transfer_state_to = Q66
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					add_contested_owner = Q66
					set_state_controller_to = Q66
				}
			}

			Q66 = {
				remove_ideas = {
					pfp_puppet_resistance_idea_1
				}
			}
		}
	}
}
