#Scripted effect to release Indochina (P16)
pfp_release_indochina = {
	hidden_effect = {
		#If resistance mechanics are enabled, formable puppets will contest and control its corresponding states.
		if = {
			limit = { has_global_flag = pfp_management_resistance_on }

			#Add constestion to formable puppet
			286 = { add_contested_owner  = P16 } #Champa
			670 = { add_contested_owner  = P16 } #Laos
			671 = { add_contested_owner  = P16 } #Tonkin
			741 = { add_contested_owner  = P16 } #Cambodia
			1017 = { add_contested_owner  = P16 } #Trung Bo

			#Set formable puppet as controller of contested state
			every_state = {
				limit = { has_contested_owner = P16 }
				
				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = { 
					limit = { is_owned_by = ROOT }
					transfer_state_to = P16
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					set_state_controller_to  = P16
				}
			}
		}
		#If resistance mechanics are disabled, formable puppets will control and core its corresponding states.
		else = {
			#Add cores to formable puppet
			286 = { add_core_of = P16 } #Champa
			670 = { add_core_of = P16 } #Laos
			671 = { add_core_of = P16 } #Tonkin
			741 = { add_core_of = P16 } #Cambodia
			1017 = { add_core_of = P16 } #Trung Bo

			#Set formable puppet as owner and controller of core state
			every_state = {
				limit = { is_core_of = P16 }

				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = { 
					limit = { is_owned_by = ROOT }

					add_contested_owner = P16
					transfer_state_to = P16
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					add_contested_owner = P16
					set_state_controller_to = P16
				}
			}
		}

		#Transfer all technology from ROOT to formable puppet
		P16 = { pfp_transfer_technology = yes }

		#Set global flag to prevent future decision use
		set_global_flag = pfp_indochina_flag

		#Send event to all nations about the formable puppet
		set_temp_variable = { temp_country_tag = P16 }
	}
}
