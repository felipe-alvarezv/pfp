#Scripted effect to release Scotland (P81)
pfp_release_scotland = {
	hidden_effect = {
		#If resistance mechanics are enabled, formable puppets will contest and control its corresponding states.
		if = {
			limit = { has_global_flag = pfp_management_resistance_enabled }

			#Add constestion to formable puppet
			120 = { add_contested_owner  = P81 } #Scottish Highlands
			121 = { add_contested_owner  = P81 } #Lothian
			133 = { add_contested_owner  = P81 } #Lanark
			136 = { add_contested_owner  = P81 } #Aberdeenshire

			#Set formable puppet as controller of contested state
			every_state = {
				limit = { has_contested_owner = P81 }
				
				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = { 
					limit = { is_owned_by = ROOT }
					transfer_state_to = P81
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					set_state_controller_to  = P81
				}
			}

			#Set custom focus tree to combat resistance
			P81 = { load_focus_tree = pfp_formable_puppet_focus }
		}
		#If resistance mechanics are disabled, formable puppets will control and core its corresponding states.
		else = {
			#Add cores to formable puppet
			120 = { add_core_of = P81 } #Scottish Highlands
			121 = { add_core_of = P81 } #Lothian
			133 = { add_core_of = P81 } #Lanark
			136 = { add_core_of = P81 } #Aberdeenshire

			#Set formable puppet as owner and controller of core state
			every_state = {
				limit = { is_core_of = P81 }

				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = { 
					limit = { is_owned_by = ROOT }

					add_contested_owner = P81
					transfer_state_to = P81
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					add_contested_owner = P81
					set_state_controller_to = P81
				}
			}
		}	}
}
