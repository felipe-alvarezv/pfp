pfp_category = {
    pfp_greenland = {
        allowed = {
            NOT = {
                original_tag = GRN #Greenland
            }
        }
        available = {
            is_subject = no
            #Greenland
            custom_trigger_tooltip = {
                tooltip = pfp_greenland_required_states_greenland
                has_full_control_of_state = 101 #Greenland
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 101 #Greenland
            }
            NOT = { has_global_flag = pfp_greenland_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 101 #Greenland
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_greenland_transfer_capital
            custom_effect_tooltip = pfp_greenland_puppet
            custom_effect_tooltip = pfp_greenland_occupation_type
            custom_effect_tooltip = pfp_greenland_state_controller

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                101 = { transfer_state_to = P85 } #Greenland

                #Formable puppet becomes a puppet of ROOT
                puppet = P85

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = P85 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_color = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_greenland = yes

                #Transfer all technology from ROOT to formable puppet
                P85 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_greenland_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}