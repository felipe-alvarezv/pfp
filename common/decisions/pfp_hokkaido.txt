pfp_category = {
    pfp_hokkaido = {
        allowed = {
            NOT = {
                original_tag = JAP #Japan
            }
        }
        available = {
            is_subject = no
            #Hokkaido
            custom_trigger_tooltip = {
                tooltip = pfp_hokkaido_required_states_hokkaido
                has_full_control_of_state = 536 #Hokkaido
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 536 #Hokkaido
            }
            NOT = { has_global_flag = pfp_hokkaido_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 536 #Hokkaido
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_hokkaido_core_states
            custom_effect_tooltip = pfp_hokkaido_transfer_states
            custom_effect_tooltip = pfp_hokkaido_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                536 = { transfer_state_to = P99 } #Hokkaido

                #Formable puppet becomes a puppet of ROOT
                puppet = P99

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = P99 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_colour = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_hokkaido = yes

                #Transfer all technology from ROOT to formable puppet
                P99 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_hokkaido_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}