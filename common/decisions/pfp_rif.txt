pfp_category = {
	pfp_rif = {
		allowed = {
			NOT = {
				original_tag = RIF #Republic of the Rif
			}
		}
		available = {
			is_subject = no
			#Rif
			custom_trigger_tooltip = {
				tooltip = pfp_rif_required_states_spanish_africa
				has_full_control_of_state = 290 #Spanish Africa
			}
		}
		visible = {
			OR = {
				has_full_control_of_state = 290 #Spanish Africa
			}
			NOT = { has_global_flag = pfp_rif_flag }
		}
		highlight_states = {
			highlight_state_targets = {
				state = 290 #Spanish Africa
			}
		}
		icon = generic_military
		complete_effect = {
			custom_effect_tooltip = pfp_rif_transfer_capital
			custom_effect_tooltip = pfp_rif_puppet
			custom_effect_tooltip = pfp_rif_occupation_type
			custom_effect_tooltip = pfp_rif_state_controller

			#Create formable puppet with all its states and then puppet
			hidden_effect = {
				#Transfer control and ownership of capital to formable puppet
				290 = { transfer_state_to = Q73 } #Spanish Africa

				#Formable puppet becomes a puppet of ROOT
				puppet = Q73

				#Code to assign generic portraits and set puppet color
				set_temp_variable = { pfp_portrait_tag = Q73 }
				pfp_set_generic_portrait = yes
				pfp_set_autonomy_color = yes
				var:pfp_portrait_tag = {
					pfp_set_puppet_ideology = yes
					if = {
						limit = {
							OR = {
								has_autonomy_state = pfp_autonomy_military_occupation
								has_autonomy_state = pfp_autonomy_military_occupation_painted
							}
						}
						pfp_set_use_overlord_flag = yes
					}
				}

				#Formable puppet will core or contest its designated territory
				pfp_release_rif = yes

				#Transfer all technology from ROOT to formable puppet
				Q73 = { pfp_transfer_technology = yes }

				#Set global flag to prevent future decision use
				set_global_flag = pfp_rif_flag
			}
		}
		ai_will_do = { factor = 0 }
	}
}
