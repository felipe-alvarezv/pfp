pfp_category = {
    pfp_montenegro = {
        allowed = {
            NOT = {
                original_tag = MNT #Montenegro
                original_tag = YUG #Yugoslavia
            }
        }
        available = {
            is_subject = no
            #Montenegro
            custom_trigger_tooltip = {
                tooltip = pfp_montenegro_required_states_montenegro
                has_full_control_of_state = 105 #Montenegro
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 105 #Montenegro
            }
            NOT = { has_global_flag = pfp_montenegro_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 105 #Montenegro
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_montenegro_core_states
            custom_effect_tooltip = pfp_montenegro_transfer_states
            custom_effect_tooltip = pfp_montenegro_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                105 = { transfer_state_to = P90 } #Montenegro

                #Code to assign generic portraits and set puppet autonomy
                set_temp_variable = { pfp_portrait_tag = P90 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_colour = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_montenegro = yes

                #Transfer all technology from ROOT to formable puppet
                P90 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_montenegro_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}