pfp_category = {
    pfp_henan = {
        allowed = {
            NOT = {
                original_tag = CHI #China
                original_tag = PRC #Chinese Soviet Republic
            }
        }
        available = {
            is_subject = no
            #Henan
            custom_trigger_tooltip = {
                tooltip = pfp_henan_required_states_henan
                has_full_control_of_state = 607 #Henan
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 607 #Henan
            }
            NOT = { has_global_flag = pfp_henan_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 607 #Henan
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_henan_transfer_capital
            custom_effect_tooltip = pfp_henan_puppet
            custom_effect_tooltip = pfp_henan_occupation_type
            custom_effect_tooltip = pfp_henan_state_controller

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                607 = { transfer_state_to = Q12 } #Henan

                #Formable puppet becomes a puppet of ROOT
                puppet = Q12

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = Q12 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_color = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_henan = yes

                #Transfer all technology from ROOT to formable puppet
                Q12 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_henan_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}