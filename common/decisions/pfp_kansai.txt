pfp_category = {
    pfp_kansai = {
        allowed = {
            NOT = {
                original_tag = JAP #Japan
            }
        }
        available = {
            is_subject = no
            #Kansai
            custom_trigger_tooltip = {
                tooltip = pfp_kansai_required_states_kansai
                has_full_control_of_state = 531 #Kansai
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 531 #Kansai
            }
            NOT = { has_global_flag = pfp_kansai_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 531 #Kansai
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_kansai_core_states
            custom_effect_tooltip = pfp_kansai_transfer_states
            custom_effect_tooltip = pfp_kansai_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                pfp_release_kansai = yes
                set_autonomy = { target = Q04 autonomy_state = pfp_autonomy_military_occupation }

                #Code to assign generic portraits
                set_temp_variable = { pfp_portrait_tag = Q04 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_colour = yes
            }
        }
        ai_will_do = { factor = 0 }
    }
}