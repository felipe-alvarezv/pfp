pfp_category = {
    pfp_shikoku = {
        allowed = {
            NOT = {
                original_tag = JAP #Japan
            }
        }
        available = {
            is_subject = no
            #Shikoku
            custom_trigger_tooltip = {
                tooltip = pfp_shikoku_required_states_shikoku
                has_full_control_of_state = 530 #Shikoku
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 530 #Shikoku
            }
            NOT = { has_global_flag = pfp_shikoku_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 530 #Shikoku
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_shikoku_core_states
            custom_effect_tooltip = pfp_shikoku_transfer_states
            custom_effect_tooltip = pfp_shikoku_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                530 = { transfer_state_to = Q06 } #Shikoku

                #Formable puppet becomes a puppet of ROOT
                puppet = Q06

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = Q06 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_color = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_shikoku = yes

                #Transfer all technology from ROOT to formable puppet
                Q06 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_shikoku_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}