pfp_category = {
    pfp_sardinia = {
        allowed = {
            NOT = {
                original_tag = ITA #Italy
                original_tag = SPM #Sardinia-Piedmont
            }
        }
        available = {
            is_subject = no
            #Sardinia
            custom_trigger_tooltip = {
                tooltip = pfp_sardinia_required_states_sardinia
                has_full_control_of_state = 114 #Sardinia
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 114 #Sardinia
            }
            NOT = { has_global_flag = pfp_sardinia_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 114 #Sardinia
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_sardinia_core_states
            custom_effect_tooltip = pfp_sardinia_transfer_states
            custom_effect_tooltip = pfp_sardinia_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                pfp_release_sardinia = yes
                set_autonomy = { target = P94 autonomy_state = autonomy_military_occupation }

                #Code to assign generic portraits
                set_temp_variable = { pfp_portrait_tag = P94 }
                pfp_set_generic_portrait = yes
            }
        }
        ai_will_do = { factor = 0 }
    }
}