pfp_category = {
    pfp_sardinia = {
        allowed = {
            NOT = {
                original_tag = ITA #Italy
                original_tag = SPM #Sardinia-Piedmont
            }
        }
        available = {
            is_subject = no
            #Sardinia
            custom_trigger_tooltip = {
                tooltip = pfp_sardinia_required_states_sardinia
                has_full_control_of_state = 114 #Sardinia
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 114 #Sardinia
            }
            NOT = { has_global_flag = pfp_sardinia_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 114 #Sardinia
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_sardinia_transfer_capital
            custom_effect_tooltip = pfp_sardinia_puppet
            custom_effect_tooltip = pfp_sardinia_occupation_type
            custom_effect_tooltip = pfp_sardinia_state_controller

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                114 = { transfer_state_to = P94 } #Sardinia

                #Formable puppet becomes a puppet of ROOT
                puppet = P94

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = P94 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_color = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_sardinia = yes

                #Transfer all technology from ROOT to formable puppet
                P94 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_sardinia_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}