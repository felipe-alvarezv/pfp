pfp_category = {
    pfp_tuscany = {
        allowed = {
            NOT = {
                original_tag = ITA #Italy
                original_tag = TOS #Tuscany
            }
        }
        available = {
            is_subject = no
            #Tuscany
            custom_trigger_tooltip = {
                tooltip = pfp_tuscany_required_states_tuscany
                has_full_control_of_state = 162 #Tuscany
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 162 #Tuscany
            }
            NOT = { has_global_flag = pfp_tuscany_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 162 #Tuscany
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_tuscany_transfer_capital
            custom_effect_tooltip = pfp_tuscany_puppet
            custom_effect_tooltip = pfp_tuscany_occupation_type
            custom_effect_tooltip = pfp_tuscany_state_controller

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                162 = { transfer_state_to = P95 } #Tuscany

                #Formable puppet becomes a puppet of ROOT
                puppet = P95

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = P95 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_color = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_tuscany = yes

                #Transfer all technology from ROOT to formable puppet
                P95 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_tuscany_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}