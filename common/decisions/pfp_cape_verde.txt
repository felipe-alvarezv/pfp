pfp_category = {
	pfp_cape_verde = {
		allowed = {
			NOT = {
				original_tag = CBV #Cape Verde
			}
		}
		available = {
			is_subject = no
			#Cape Verde
			custom_trigger_tooltip = {
				tooltip = pfp_cape_verde_required_states_cape_verde
				has_full_control_of_state = 702 #Cape Verde
			}
		}
		visible = {
			OR = {
				has_full_control_of_state = 702 #Cape Verde
			}
			NOT = { has_global_flag = pfp_cape_verde_flag }
		}
		highlight_states = {
			highlight_state_targets = {
				state = 702 #Cape Verde
			}
		}
		icon = generic_military
		complete_effect = {
			custom_effect_tooltip = pfp_cape_verde_transfer_capital
			custom_effect_tooltip = pfp_cape_verde_puppet
			custom_effect_tooltip = pfp_cape_verde_occupation_type
			custom_effect_tooltip = pfp_cape_verde_state_controller

			#Create formable puppet with all its states and then puppet
			hidden_effect = {
				#Transfer control and ownership of capital to formable puppet
				702 = { transfer_state_to = Q51 } #Cape Verde

				#Formable puppet becomes a puppet of ROOT
				puppet = Q51

				#Code to assign generic portraits and set puppet color
				set_temp_variable = { pfp_portrait_tag = Q51 }
				pfp_set_generic_portrait = yes
				pfp_set_autonomy_color = yes
				var:pfp_portrait_tag = {
					pfp_set_puppet_ideology = yes
					if = {
						limit = {
							OR = {
								has_autonomy_state = pfp_autonomy_military_occupation
								has_autonomy_state = pfp_autonomy_military_occupation_painted
							}
						}
						pfp_set_use_overlord_flag = yes
					}
				}

				#Formable puppet will core or contest its designated territory
				pfp_release_cape_verde = yes

				#Transfer all technology from ROOT to formable puppet
				Q51 = { pfp_transfer_technology = yes }

				#Set global flag to prevent future decision use
				set_global_flag = pfp_cape_verde_flag
			}
		}
		ai_will_do = { factor = 0 }
	}
}
