pfp_category = {
    pfp_wales = {
        allowed = {
            NOT = {
                original_tag = ENG #United Kingdom
                original_tag = WLS #Wales
            }
        }
        available = {
            is_subject = no
            #Wales
            custom_trigger_tooltip = {
                tooltip = pfp_wales_required_states_wales
                has_full_control_of_state = 122 #Wales
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 122 #Wales
            }
            NOT = { has_global_flag = pfp_wales_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 122 #Wales
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_wales_core_states
            custom_effect_tooltip = pfp_wales_transfer_states
            custom_effect_tooltip = pfp_wales_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                122 = { transfer_state_to = P82 } #Wales

                #Formable puppet becomes a puppet of ROOT
                puppet = P82

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = P82 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_colour = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_wales = yes

                #Transfer all technology from ROOT to formable puppet
                P82 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_wales_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}