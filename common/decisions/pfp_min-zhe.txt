pfp_category = {
    pfp_min-zhe = {
        allowed = {
            NOT = {
                original_tag = CHI #China
                original_tag = PRC #Chinese Soviet Republic
            }
        }
        available = {
            is_subject = no
            #Fujian
            custom_trigger_tooltip = {
                tooltip = pfp_min-zhe_required_states_fujian
                has_full_control_of_state = 595 #Fujian
            }
            #Zhejiang
            custom_trigger_tooltip = {
                tooltip = pfp_min-zhe_required_states_zhejiang
                has_full_control_of_state = 596 #Zhejiang
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 595 #Fujian
                has_full_control_of_state = 596 #Zhejiang
            }
            NOT = { has_global_flag = pfp_min-zhe_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 595 #Fujian
                state = 596 #Zhejiang
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_min-zhe_core_states
            custom_effect_tooltip = pfp_min-zhe_transfer_states
            custom_effect_tooltip = pfp_min-zhe_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                595 = { transfer_state_to = Q15 } #Fujian

                #Formable puppet becomes a puppet of ROOT
                puppet = Q15

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = Q15 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_colour = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_min-zhe = yes

                #Transfer all technology from ROOT to formable puppet
                Q15 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_min-zhe_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}