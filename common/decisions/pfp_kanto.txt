pfp_category = {
    pfp_kanto = {
        allowed = {
            NOT = {
                original_tag = JAP #Japan
            }
        }
        available = {
            is_subject = no
            #Kanto
            custom_trigger_tooltip = {
                tooltip = pfp_kanto_required_states_kanto
                has_full_control_of_state = 282 #Kanto
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 282 #Kanto
            }
            NOT = { has_global_flag = pfp_kanto_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 282 #Kanto
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_kanto_transfer_capital
            custom_effect_tooltip = pfp_kanto_puppet
            custom_effect_tooltip = pfp_kanto_occupation_type
            custom_effect_tooltip = pfp_kanto_state_controller

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                282 = { transfer_state_to = Q02 } #Kanto

                #Formable puppet becomes a puppet of ROOT
                puppet = Q02

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = Q02 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_color = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_kanto = yes

                #Transfer all technology from ROOT to formable puppet
                Q02 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_kanto_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}