pfp_category = {
    pfp_kanto = {
        allowed = {
            NOT = {
                original_tag = JAP #Japan
            }
        }
        available = {
            is_subject = no
            #Kanto
            custom_trigger_tooltip = {
                tooltip = pfp_kanto_required_states_kanto
                has_full_control_of_state = 282 #Kanto
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 282 #Kanto
            }
            NOT = { has_global_flag = pfp_kanto_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 282 #Kanto
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_kanto_core_states
            custom_effect_tooltip = pfp_kanto_transfer_states
            custom_effect_tooltip = pfp_kanto_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                pfp_release_kanto = yes
                set_autonomy = { target = Q02 autonomy_state = pfp_autonomy_military_occupation }

                #Code to assign generic portraits
                set_temp_variable = { pfp_portrait_tag = Q02 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_colour = yes
            }
        }
        ai_will_do = { factor = 0 }
    }
}