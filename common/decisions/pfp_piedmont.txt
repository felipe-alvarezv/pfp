pfp_category = {
    pfp_piedmont = {
        allowed = {
            NOT = {
                original_tag = ITA #Italy
                original_tag = SPM #Sardinia-Piedmont
            }
        }
        available = {
            is_subject = no
            #Piedmont
            custom_trigger_tooltip = {
                tooltip = pfp_piedmont_required_states_piedmont
                has_full_control_of_state = 158 #Piedmont
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 158 #Piedmont
            }
            NOT = { has_global_flag = pfp_piedmont_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 158 #Piedmont
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_piedmont_core_states
            custom_effect_tooltip = pfp_piedmont_transfer_states
            custom_effect_tooltip = pfp_piedmont_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                158 = { transfer_state_to = P93 } #Piedmont

                #Formable puppet becomes a puppet of ROOT
                puppet = P93

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = P93 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_colour = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_piedmont = yes

                #Transfer all technology from ROOT to formable puppet
                P93 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_piedmont_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}